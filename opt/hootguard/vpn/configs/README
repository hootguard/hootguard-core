VPN Client Configurations
This folder is used to store the configuration files for VPN clients in the HootGuard system.

Important:
Do not delete this folder, even if it is empty. It is crucial for managing VPN clients.
Client configuration files are stored here after being generated by the VPN client creation scripts.
